<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context-3.0.xsd">

	<!-- 任务配置 -->
	<bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
		<property name="triggers">
			<list>	<!-- 任务列表 -->
				<ref bean="checkOnlinePlayerScheduleTrigger" />
				<ref bean="arrangePlayerRegionScheduleTrigger"/>
			</list>
		</property>
		<property name="autoStartup" value="true" />
	</bean>

	<!-- 检查用户在线状态 -->
	<bean id="checkOnlinePlayerScheduleTrigger"
		class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
		<property name="jobDetail" ref="checkOnlinePlayerScheduleDetail" />
		<property name="cronExpression" value="0 */3  * * * ?" /><!-- 每隔3分钟钟触发一次 -->
	</bean>
	<bean id="checkOnlinePlayerScheduleDetail"
		class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
		<property name="targetObject" ref="checkOnlinePlayerSchedule" />
		<property name="targetMethod" value="run" />
		<property name="concurrent" value="false" />	<!-- 是否允许任务并发执行。当值为false时，表示必须等到前一个线程处理完毕后才再启一个新的线程 -->
	</bean>
	<bean id="checkOnlinePlayerSchedule" class="com.markany.gameboss.player.schedule.CheckOnlinePlayer">
		<property name="redisOperator" ref="redisOperator"/>
		<property name="playerService" ref="playerService"/>
	</bean>
	<!-- 更新用户区域 -->
	<bean id="arrangePlayerRegionScheduleTrigger"
		class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
		<property name="jobDetail" ref="arrangePlayerRegionScheduleDetail" />
		<property name="cronExpression" value="0 0 3 * * ?" /><!-- 每天凌晨3点修改 -->
	</bean>
	<bean id="arrangePlayerRegionScheduleDetail"
		class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
		<property name="targetObject" ref="arrangePlayerRegionSchedule" />
		<property name="targetMethod" value="run" />
		<property name="concurrent" value="false" />	<!-- 是否允许任务并发执行。当值为false时，表示必须等到前一个线程处理完毕后才再启一个新的线程 -->
	</bean>
	<bean id="arrangePlayerRegionSchedule" class="com.markany.gameboss.player.schedule.ArrangePlayerRegion">
		<property name="playerService" ref="playerService"/>
	</bean>

</beans>